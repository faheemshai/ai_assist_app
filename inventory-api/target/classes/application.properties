server.port=8080

# ---------------------------------------------------------------------------
# H2 in-memory datasource
# ---------------------------------------------------------------------------
spring.datasource.url=jdbc:h2:mem:inventorydb;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# H2 web console — convenient for dev, must be disabled in production
spring.h2.console.enabled=true

# ---------------------------------------------------------------------------
# VULNERABILITY: Hardcoded secret in source-controlled config file.
#
# Any developer, CI pipeline, or container image inspector can read this
# value in plain text.  Secrets must never be committed to source control.
#
# Fix: remove from this file and inject at runtime via:
#   • An environment variable  (API_KEY env var → @Value("${API_KEY}"))
#   • A Kubernetes Secret mounted as an env var or volume
#   • A secrets manager (HashiCorp Vault, AWS Secrets Manager, etc.)
# ---------------------------------------------------------------------------
api.key=sk_live_4a7f2c91d83e0b56f1a2c3d4e5f60718
