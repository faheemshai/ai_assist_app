server.port=8080

# H2 in-memory datasource
spring.datasource.url=jdbc:h2:mem:inventorydb;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# H2 console (dev convenience — should be disabled in production)
spring.h2.console.enabled=true

# VULNERABILITY: Hardcoded API key stored in plain-text config.
# Anyone with access to the repo, the container image, or the running pod
# can read this secret.  Fix: inject via an environment variable or a
# secrets manager (Vault, AWS Secrets Manager, Kubernetes Secrets).
app.api-key=a3f8c2d1-9b47-4e6f-82aa-deadbeef0001
app.admin-token=sup3rS3cr3tAdminT0ken!

# Internal service credentials
app.db.backup-password=Backup$Pass2024
